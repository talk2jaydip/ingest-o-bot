# Core Azure dependencies
azure-ai-documentintelligence>=1.0.0
azure-search-documents>=11.4.0
azure-storage-blob>=12.19.0
azure-identity>=1.15.0
azure-keyvault-secrets>=4.7.0
azure-core>=1.29.0

# OpenAI
openai>=1.10.0

# Document processing - PDF and Office formats
# MarkItDown: Unified converter for PDFs, Office docs (DOCX/PPTX/DOC), and more
# Supports PDF extraction in offline mode (replaces pypdf/pymupdf)
# Note: DOC files require LibreOffice installed on system (not a Python package)
markitdown>=0.0.1

# Image processing
Pillow>=10.0.0

# Optional: Legacy PDF libraries (no longer used by default)
# pypdf>=4.0.0
# pymupdf>=1.23.0

# Utilities
python-dotenv>=1.0.0
aiohttp>=3.9.0
tiktoken>=0.5.0
tenacity>=8.2.0

# Optional for rich logging
rich>=13.0.0

